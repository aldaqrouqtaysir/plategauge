# Experimental camera publication boundary

This document describes the decision; it does **not** record approval.
Historical Gate D permission covered the benchmark only and cannot authorize
this camera release. Do not create `release/camera/approval.json` from an old
approval, an inferred reviewer action or a successful test result.

The requested bounded permission covers:

- The exact tested app `ca88b440636b8028e60a1e79ae6a51b0539a37ca` and its
  hash-inventoried 46-file bundle, published as an experimental camera workflow.
- Separately reviewed release operations, a new approval-only tag, prerelease
  assets, Pages deployment, exact-file and generated-input live verification,
  rollback to the preserved v1.0.2 bundle, and weekly smoke routing.
- The public repository's project-first documentation, with limitations and
  historical authorship/contribution records retained.

It does not cover a new model, improved-accuracy claim, production suitability,
new datasets or experiments, moved old tags, private research publication,
physical-device validation claims, or application use. The previous paired
model comparison and failed acceptance gates remain unchanged.

The approval record binds the operational source's exact parent commit and the
SHA-256 hashes of both inventories, both distribution archives and compact
verification evidence. Its date and approver must reflect the real decision.
The approval-only child must change no other path. Workflows additionally
require the new tag to resolve to that child. Subsequent application or bound
artifact changes need a new decision and release; never overwrite this tag.
